# include<stdio.h>

int main()
{
    int x,sum;
    printf("enter number:");
    scanf("%d",&x);
    sum

    
  if(sum==x)
  { 
    printf("Armstrong number");
  }
  else{
    printf("not a Armstrong number");
  }

    return 0;
}